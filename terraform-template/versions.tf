terraform {
  required_version = ">= 1.5.0"

  required_providers {
    # Declare the target cloud provider in the copied project, for example:
    #
    # arvan = {
    #   source  = "arvancloud/arvan"
    #   version = "0.6.4"
    # }
  }
}
